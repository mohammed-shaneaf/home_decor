import 'package:flutter/material.dart';

class SplashTwoViewBody extends StatelessWidget {
  const SplashTwoViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [

      ],
    );
  }
}
